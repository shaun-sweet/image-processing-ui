import React from 'react'
import 'styles/imageOutputArea.css';

const ImageOutputArea = (props) => {
  return <div className="imageOutputArea">{props.children}</div>
}

export default ImageOutputArea;
