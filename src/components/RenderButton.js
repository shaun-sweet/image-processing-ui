import React from 'react'
import 'styles/renderButton.css';

const RenderButton = (props) => {
  return (
    <input type='submit' value='Render' className="render-button" />
  )
}
export default RenderButton;
